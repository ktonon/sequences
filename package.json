{
  "name": "d2l-sequences",
  "description": "",
  "private": true,
  "scripts": {
    "postinstall": "polymer install",
    "lint:fix": "eslint *.html demo/*.html test/*.html --fix",
    "lint:html": "eslint *.html demo/*.html test/*.html",
    "lint": "npm run lint:html && polymer lint --input ./**/*.html",
    "pretest": "npm run lint",
    "test": "polymer test --skip-plugin sauce",
    "serve": "polymer analyze > analysis.json && polymer serve"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/BrightspaceComponents/sequences"
  },
  "author": "D2L Corporation",
  "license": "Apache-2.0",
  "devDependencies": {
    "eslint": "^4.19.1",
    "eslint-config-brightspace": "^0.4.0",
    "eslint-plugin-html": "^4.0.1",
    "polymer-cli": "^1.5.7"
  }
}
